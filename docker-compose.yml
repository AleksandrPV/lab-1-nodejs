version: '3.8'

services:
  app:
    build: .
    container_name: nodejs-app
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules 